class paper
{
    constructor(x,y,r)
}
var options={
isStatic:false,
restitution:0.3,
friction:0,
density:1.2

   
,thisimage=loadImage("paper.png"),
this:x=x
,this:y=y
,this:image=loadImage("sprites/paper.png"),
World,add(world,thisbody);
}
 display();
 push();
translate(this.body.position.x,this.body.position.y);
rotate(angle);
imageMode(CENTER);
pop();

 